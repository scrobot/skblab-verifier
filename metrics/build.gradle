apply plugin: 'org.springframework.boot'
apply plugin: 'com.bmuschko.docker-spring-boot-application'

dependencies { 
 
}

docker {
  springBootApplication {
    baseImage = 'adoptopenjdk/openjdk8'
//    ports = [4000, 9095]
//    tag = "reactive-pacman-metrics-server:${project.version}"
  }
}